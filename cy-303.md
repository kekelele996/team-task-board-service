<!-- 改编自：综合实用主题项目提示词/cy-303/cy-303.md；后端由 Node.js + Express 改造为 Go 1.22 + Gin + GORM -->
请根据以下要求生成项目代码和 README。

## 项目主要需求

**项目概述**：团队任务看板是一个支持拖拽操作的协作式项目管理工具，采用看板（Kanban）视图展示任务状态流转，适用于敏捷开发和小团队协作。
**项目类型**：全栈 Web 应用

### 功能需求

1. **工作区与看板管理**：用户可创建多个工作区（Workspace），每个工作区包含多个看板（Board）。看板采用经典的三列布局：待办（To Do）、进行中（In Progress）、已完成（Done），支持自定义添加更多列（如"审核中"、"已归档"）。

2. **拖拽任务卡片**：任务以卡片形式展示在看板列中，支持在同列内拖拽排序，以及跨列拖拽改变状态。拖拽操作实时更新任务状态，操作流畅无卡顿（使用原生 HTML5 Drag and Drop 或 @hello-pangea/dnd）。

3. **任务详情与属性**：点击卡片展开任务详情侧边栏/弹窗，包含：标题、描述（Markdown 支持）、负责人（单选）、标签（多选，彩色标签）、优先级（高/中/低，带颜色标识）、截止日期、子任务清单（可勾选完成）、附件上传、评论记录。

4. **成员协作与权限**：工作区可邀请成员加入，支持设置成员角色（管理员/编辑者/观察者）。管理员可管理工作区设置和成员。编辑者可创建、编辑、移动任务。观察者仅可查看。任务分配后，被分配人收到通知。

5. **任务筛选与搜索**：支持按负责人、标签、优先级、截止日期筛选看板中的任务。支持按关键词搜索任务标题和描述。筛选结果在看板上高亮显示匹配卡片，其余卡片半透明。

6. **活动日志与通知**：记录看板中所有操作日志（谁创建了任务、移动了任务、修改了什么）。用户可查看与自己相关的活动通知（被分配任务、任务被评论、截止日期临近提醒）。

7. **看板统计视图**：除看板视图外，提供简洁的数据统计面板：各列任务数量、成员任务分布饼图、本周完成任务趋势折线图、逾期任务列表。

## 技术要求

- **英文短名（用于 Docker Compose）**：`gbkanban`
- **前端端口**：`18503`
- **后端端口**：`19503`
- **数据库端口**：`57503`
- **前端框架**：React 18 + TypeScript
- **UI 库**：Ant Design
- **构建工具**：Vite
- **拖拽库**：@hello-pangea/dnd
- **后端框架**：Go 1.22 + Gin
- **数据库**：MySQL 8.0
- **ORM**：GORM
- **数据库驱动**：gorm.io/driver/mysql
- **认证方式**：JWT，使用 github.com/golang-jwt/jwt/v5，保留管理员/编辑者/观察者角色 RBAC
- **其他依赖**：前端 Axios、Zustand（状态管理）、dayjs、lodash；后端 github.com/gin-gonic/gin、gorm.io/gorm、gorm.io/driver/mysql、github.com/golang-jwt/jwt/v5
## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。


## 部署要求

这是前后端分离项目，必须支持 Docker Compose 一键部署。

前端代码放在 `frontend/`：
- 页面，组件，路由, 状态管理, API 请求, 类型定义, 样式和资源等放在对应的子文件夹

后端代码放在 `backend/`，采用 Go 标准布局：
```
backend/
├── go.mod
├── cmd/server/main.go
└── internal/
    ├── config/
    ├── model/
    ├── repository/
    ├── service/
    ├── handler/
    ├── router/
    ├── middleware/
    ├── dto/
    └── constants/
```
- 数据库脚本放在 `database/`。

后端代码组织约束（Go 等价表述）：
- handler 只负责 HTTP 请求参数绑定、响应和错误转换，不直接写业务逻辑
- service 负责业务逻辑，repository 负责数据访问，model 负责 GORM 数据模型，dto 负责请求/响应结构体
- middleware 负责 JWT 鉴权和角色 RBAC 校验等横切逻辑，constants 统一维护错误码、常量
- 各层通过 Go 构造函数注入依赖，禁止把职责堆在 main.go，必须触达多个文件

后端本地构建与运行命令：
- 依赖安装：`go mod tidy`
- 编译检查：`go build ./...`
- 本地运行：`go run ./cmd/server`

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=gbkanban`
- `frontend/Dockerfile`：多阶段构建，Nginx 托管
- `backend/Dockerfile`：Go 多阶段构建
- `frontend/nginx.conf`
- `.env.example`

`backend/Dockerfile` 必须采用 Go 多阶段构建：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: gbkanban`
- 所有服务使用 `container_name`，带 `${COMPOSE_PROJECT_NAME:-gbkanban}` 前缀
- 数据库数据使用命名卷持久化
- 后端服务内部端口统一为 `8080`
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `http://backend:8080/`）
- 数据库服务配置 `healthcheck`，后端 `depends_on: { db: { condition: service_healthy } }`
- 后端服务配置 `healthcheck`（后端提供 `/health` 健康检查接口），前端 `depends_on` 后端健康
- 暴露端口：前端 `${FRONTEND_PORT:-18503}:80`，后端 `${BACKEND_PORT:-19503}:8080`，数据库 `${DB_PORT:-57503}:3306`
- 所有可变配置通过环境变量注入
- 支持 `docker compose up -d` 启动

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=gbkanban`
- `DB_NAME` / `DB_USER` / `DB_PASSWORD` / `DB_ROOT_PASSWORD`
- `JWT_SECRET`
- `FRONTEND_PORT`、`BACKEND_PORT`、`DB_PORT`

Nginx 反向代理要求：
- `location /api/` 反向代理到 `http://backend:8080/`
- `try_files $uri $uri/ /index.html;` 支持 SPA 路由

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式
- 访问地址
- 技术栈（表格展示，后端技术栈写为 Go 1.22 + Gin + GORM）
- 项目目录结构（代码块）
- 环境变量说明
- Docker 部署说明
- License

后端本地启动命令为 `go mod tidy` 和 `go run ./cmd/server`。
首次启动前执行 `cp .env.example .env`。

## Git 要求

代码编写完成后初始化 Git 仓库：
```bash
git init
git add -A
git commit -m "init: 项目初始化"
```

创建 `.gitignore` 文件，内容包含：
```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
coverage/
backend/bin/
backend/server
*.exe
*.test
coverage.out
```
